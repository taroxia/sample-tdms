<UserControl x:Class="WpfUI.Features.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="400">
    <Border Padding="20" Background="#F3F3F3" CornerRadius="8">
        <StackPanel>
            <TextBlock Text="システムダッシュボード" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="ステータス:" Foreground="Gray"/>
            <TextBlock Text="{Binding Status}" FontSize="18" Margin="0,0,0,10"/>

            <TextBlock Text="CPU 使用率:" Foreground="Gray"/>
            <ProgressBar Value="{Binding CpuUsage}" Height="20" Margin="0,5,0,0"/>
            <TextBlock Text="{Binding CpuUsage, StringFormat={}{0}%}" HorizontalAlignment="Right"/>
        </StackPanel>
    </Border>
</UserControl>
 