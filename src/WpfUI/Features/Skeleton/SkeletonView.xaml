<core:ViewBase x:TypeArguments="local:SkeletonViewModel"
               x:Class="WpfUI.Features.Skeleton.SkeletonView"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
               xmlns:local="clr-namespace:WpfUI.Features.Skeleton"
               xmlns:core="clr-namespace:WpfUI.Core.Base"
               mc:Ignorable="d" 
               d:DesignHeight="600" d:DesignWidth="800">

  <Grid Background="#1A1A1C">
    <StackPanel Margin="15">
      <TextBlock Text="System Metrics Stream (Right-Bottom Pane)" Foreground="White" FontSize="15" FontWeight="Bold" Margin="0,0,0,10"/>
      <Border Background="#252526" CornerRadius="6" BorderBrush="#3F3F46" BorderThickness="1" Padding="15">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>

          <StackPanel Grid.Column="0">
            <TextBlock Text="Active Pipeline Status:" Foreground="#A1A1AA" FontSize="11"/>
            <TextBlock Text="{Binding ObservedTarget.Value, FallbackValue='Pipeline Idle'}" 
                       Foreground="#4A90E2" FontSize="16" FontWeight="SemiBold" Margin="0,4,0,0"/>
          </StackPanel>

          <Button Grid.Column="1" Content="Clear Core State" Command="{Binding ClearStateCommand}" 
                  Padding="10,5" Background="#3E3E42" Foreground="White" BorderThickness="0" VerticalAlignment="Center"/>
        </Grid>
      </Border>
    </StackPanel>
  </Grid>
</core:ViewBase>
