<core:ViewBase x:TypeArguments="local:SkeletonViewModel"
               x:Class="WpfUI.Features.Skeleton.SkeletonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:explorer="clr-namespace:WpfUI.Features.Skeleton.Explorer"
             xmlns:documents="clr-namespace:WpfUI.Features.Skeleton.Documents"
               xmlns:local="clr-namespace:WpfUI.Features.Skeleton"
               xmlns:core="clr-namespace:WpfUI.Core.Base"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="250"/>
      <ColumnDefinition Width="5"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <explorer:SkeletonExpView Grid.Column="0" DataContext="{Binding ExplorerViewModel}"/>

    <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="#2D2D30"/>

    <documents:SkeletonDocView Grid.Column="2" DataContext="{Binding DocumentsViewModel}"/>
  </Grid>
</core:ViewBase>
