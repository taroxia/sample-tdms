<core:ViewBase x:TypeArguments="local:WaveExplorerViewModel"
               x:Class="WpfUI.Features.Wave.Explorer.WaveExplorerView"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               xmlns:local="clr-namespace:WpfUI.Features.Wave.Explorer"
               xmlns:core="clr-namespace:WpfUI.Core.Base"
               mc:Ignorable="d"
               d:DesignHeight="300" d:DesignWidth="400">
  <!--StackPanel>
    <TextBlock Text="{Binding FileName}" FontSize="24" FontWeight="Bold"/>
  </StackPanel -->

  <!-- Microsoft.Xaml.Behaviors.Wpf -->
  
  <Grid AllowDrop="True" Background="Transparent">
    <i:Interaction.Triggers>
      <i:EventTrigger EventName="PreviewDragOver">
        <i:InvokeCommandAction Command="{Binding PreviewDragOverCommand}" PassEventArgsToCommand="True" />
      </i:EventTrigger>
      <i:EventTrigger EventName="Drop">
        <i:InvokeCommandAction Command="{Binding DropCommand}" PassEventArgsToCommand="True" />
      </i:EventTrigger>
    </i:Interaction.Triggers>

    <TextBlock Text="{Binding DroppedFilePath.Value}" 
                   VerticalAlignment="Center" HorizontalAlignment="Center" 
                   TextWrapping="Wrap" />
  </Grid>

  <!-- StackPanel Margin="20">
    <Label Content="解析対象ファイル:" FontWeight="Bold" />

    <Border BorderThickness="1" 
            BorderBrush="#CCC" 
            Background="#F9F9F9" 
            CornerRadius="4" 
            Padding="10">
      <Grid>
        <TextBlock Text="ここに TDMS ファイルをドラッグ＆ドロップしてください" 
                       Foreground="Gray"
                       FontStyle="Italic"
                       HorizontalAlignment="Center" />

        <TextBlock Text="{Binding FileName.Value}" 
                       TextTrimming="CharacterEllipsis"
                       ToolTip="{Binding FileName.Value}"
                       FontFamily="Cascadia Code, Consolas"
                       Foreground="#333" />
      </Grid><Grid AllowDrop="True" Background="Transparent">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="PreviewDragOver">
                <i:InvokeCommandAction Command="{Binding PreviewDragOverCommand}" PassEventArgsToCommand="True" />
            </i:EventTrigger>
            <i:EventTrigger EventName="Drop">
                <i:InvokeCommandAction Command="{Binding DropCommand}" PassEventArgsToCommand="True" />
            </i:EventTrigger>
        </i:Interaction.Triggers>
        
        <TextBlock Text="{Binding DroppedFilePath.Value}" 
                   VerticalAlignment="Center" HorizontalAlignment="Center" 
                   TextWrapping="Wrap" />
    </Grid>
    </Border><Grid AllowDrop="True" Background="Transparent">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="PreviewDragOver">
                <i:InvokeCommandAction Command="{Binding PreviewDragOverCommand}" PassEventArgsToCommand="True" />
            </i:EventTrigger>
            <i:EventTrigger EventName="Drop">
                <i:InvokeCommandAction Command="{Binding DropCommand}" PassEventArgsToCommand="True" />
            </i:EventTrigger>
        </i:Interaction.Triggers>
        
        <TextBlock Text="{Binding DroppedFilePath.Value}" 
                   VerticalAlignment="Center" HorizontalAlignment="Center" 
                   TextWrapping="Wrap" />
    </Grid>
  </StackPanel -->
</core:ViewBase>
