<core:ViewBase x:TypeArguments="local:WaveformViewModel"
               x:Class="WpfUI.Features.Waveform.WaveformView"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               xmlns:ri="clr-namespace:Reactive.Bindings.Interactivity;assembly=ReactiveProperty.WPF"
               xmlns:sp="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
               xmlns:local="clr-namespace:WpfUI.Features.Waveform"
               xmlns:core="clr-namespace:WpfUI.Core.Base"
               mc:Ignorable="d"
               d:DesignHeight="300" d:DesignWidth="400">
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <!-- use Pack URI -->
        <ResourceDictionary Source="/Features/Waveform/Assets/Icons.xaml" />
        <ResourceDictionary Source="/Features/Waveform/Assets/Styles.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </UserControl.Resources>

  <Grid Background="#0A0A0A" AllowDrop="True" Loaded="OnLoaded">
    <sp:WpfPlot x:Name="MainPlot" />

    <Grid x:Name="DropOverlay" IsHitTestVisible="False" Opacity="0.5">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="60"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="60"/>
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="*"/>
        <RowDefinition Height="40"/>
      </Grid.RowDefinitions>

      <Border Grid.Row="0" Grid.Column="0" Background="#2200FF00" BorderBrush="#00FF00" BorderThickness="1" x:Name="LeftTarget"/>
      <Border Grid.Row="0" Grid.Column="2" Background="#22FF00FF" BorderBrush="#FF00FF" BorderThickness="1" x:Name="RightTarget"/>
      <Border Grid.Row="1" Grid.Column="1" Background="#22FFFF00" BorderBrush="#FFFF00" BorderThickness="1" x:Name="XTarget"/>
    </Grid>

    <i:Interaction.Triggers>      
      <i:EventTrigger EventName="Drop">
        <i:InvokeCommandAction Command="{Binding DropToChartCommand}" PassEventArgsToCommand="True"/>
      </i:EventTrigger>
    </i:Interaction.Triggers>
  </Grid>
</core:ViewBase>
