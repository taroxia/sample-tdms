<core:ViewBase x:TypeArguments="local:WaveformExpViewModel"
               x:Class="WpfUI.Features.Waveform.Explorer.WaveformExpView"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               xmlns:local="clr-namespace:WpfUI.Features.Waveform.Explorer"
               xmlns:core="clr-namespace:WpfUI.Core.Base"
               Background="Transparent">

  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <!-- use Pack URI -->
        <ResourceDictionary Source="/Features/Waveform/Assets/Icons.xaml" />
        <ResourceDictionary Source="/Features/Waveform/Assets/Styles.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </UserControl.Resources>

  <Grid AllowDrop="True">
    <i:Interaction.Triggers>
      <i:EventTrigger EventName="Drop">
        <i:InvokeCommandAction Command="{Binding DropFileCommand}" PassEventArgsToCommand="True"/>
      </i:EventTrigger>
    </i:Interaction.Triggers>

    <Border Style="{StaticResource GlassCard}" Margin="20">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="15" Orientation="Horizontal">
          <Path Data="{StaticResource Icon.FileTdms}" Fill="#0078D4" Stretch="Uniform" Width="20" Height="20"/>
          <TextBlock Text="TDMS DATA EXPLORER" Foreground="White" FontSize="14" FontWeight="Bold" Margin="10,0"/>
        </StackPanel>

        <ListBox Grid.Row="1" 
                         ItemsSource="{Binding Channels}" 
                         SelectionMode="Extended"
                         Background="Transparent" BorderThickness="0">
          <ListBox.ItemTemplate>
            <DataTemplate>
              <Grid Margin="5">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="Auto"/>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Path Grid.Column="0" Data="{StaticResource Icon.Waveform}" Fill="#00F2FF" Stretch="Uniform" Width="16" Margin="0,0,10,0"/>
                <StackPanel Grid.Column="1">
                  <TextBlock Text="{Binding ChannelName}" Foreground="White" FontWeight="SemiBold"/>
                  <TextBlock Text="{Binding GroupName}" Foreground="#88FFFFFF" FontSize="10"/>
                </StackPanel>
                <TextBlock Grid.Column="2" Text="{Binding SampleCount, StringFormat='\{0:N0\} pts'}" Foreground="#55FFFFFF" VerticalAlignment="Center"/>
              </Grid>
            </DataTemplate>
          </ListBox.ItemTemplate>
          <i:Interaction.Triggers>
            <i:EventTrigger EventName="SelectionChanged">
              <i:InvokeCommandAction Command="{Binding SelectionChangedCommand}"
                                     CommandParameter="{Binding SelectedItems, RelativeSource={RelativeSource AncestorType=ListBox}}"/>
            </i:EventTrigger>
            
            <i:EventTrigger EventName="MouseMove">
              <i:InvokeCommandAction Command="{Binding StartDragCommand}" PassEventArgsToCommand="True"/>
            </i:EventTrigger>
          </i:Interaction.Triggers>
        </ListBox>

        <Border IsHitTestVisible="False" Visibility="{Binding IsEmpty.Value, Converter={StaticResource BoolToVis}}">
          <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="DROP TDMS FILES HERE" Foreground="#33FFFFFF" FontSize="24" FontWeight="Black"/>
          </StackPanel>
        </Border>
      </Grid>
    </Border>
  </Grid>
  <!--
  <Grid AllowDrop="True">
    <Border x:Name="DropZone" 
                Background="{StaticResource SystemAccentColorLight3}" 
                Opacity="0" CornerRadius="8" IsHitTestVisible="False"
                BorderBrush="{StaticResource SystemAccentColor}" BorderThickness="2" DashCap="Round">
      <TextBlock Text="Drop TDMS Files Here" VerticalAlignment="Center" HorizontalAlignment="Center" 
                       Foreground="{StaticResource SystemAccentColor}" FontSize="16" FontWeight="SemiBold"/>
    </Border>

    <DockPanel Margin="12">
      <TextBlock Text="DATA EXPLORER" DockPanel.Dock="Top" FontSize="12" FontWeight="Bold" 
                       Foreground="{StaticResource SubTextBrush}" Margin="4,0,0,12" />

      <TreeView ItemsSource="{Binding RootNodes}" BorderThickness="0" Background="Transparent"
                      VirtualizingStackPanel.IsVirtualizing="True">

        <i:Interaction.Behaviors>
          <i:EventTrigger Behavior="Drop">
            <i:InvokeCommandAction Command="{Binding DropCommand}" PassEventArgsToCommand="True"/>
          </i:EventTrigger>
        </i:Interaction.Behaviors>

        <TreeView.ItemTemplate>
          <HierarchicalDataTemplate ItemsSource="{Binding Children}">
            <StackPanel Orientation="Horizontal" Height="32">
              <Path Data="{Binding Icon}" Fill="{Binding IconColor}" Width="16" Stretch="Uniform" Margin="0,0,8,0"/>
              <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Foreground="White"/>
              <TextBlock Text="{Binding Info}" Margin="12,0,0,0" VerticalAlignment="Center" 
                                       Foreground="{StaticResource SubTextBrush}" FontSize="11"/>
            </StackPanel>
          </HierarchicalDataTemplate>
        </TreeView.ItemTemplate>
      </TreeView>
    </DockPanel>
  </Grid>
  -->

</core:ViewBase>
